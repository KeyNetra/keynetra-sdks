name: ⚙️ Generate SDKs

on:
  push:
    branches:
      - main
    paths:
      - openapi/keynetra.yaml
  pull_request:
    branches:
      - main
    paths:
      - openapi/keynetra.yaml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    name: 🛠️ Generate & Prepare SDKs
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: 📦 Install OpenAPI Generator
        run: |
          echo "Installing openapi-generator-cli..."
          npm install @openapitools/openapi-generator-cli
          echo "$(pwd)/node_modules/.bin" >> $GITHUB_PATH

      - name: 🚀 Run SDK Generation
        run: |
          chmod +x scripts/generate-all.sh scripts/prepare-packages.sh
          ./scripts/generate-all.sh

      - name: 💾 Commit & Push Changes
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          git config user.name "KeyNetra Engineering"
          git config user.email "business.keynetra@gmail.com"
          git add scripts templates openapi
          if git diff --cached --quiet; then
            echo "✨ No SDK changes detected."
            exit 0
          fi
          git commit -m "chore(sdk): regenerate SDKs 🤖"
          git push

      - name: 💾 Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: generated-sdks
          path: sdks

  test:
    name: 🧪 Smoke Test SDKs
    needs: generate
    runs-on: ubuntu-latest
    services:
      keynetra:
        image: docker.io/keynetra/keynetra:latest
        ports:
          - 8080:8080
        options: >-
          --health-cmd "(python3 -c 'import socket; s = socket.socket(socket.AF_INET, socket.SOCK_STREAM); s.connect((\"localhost\", 8080))' || python -c 'import socket; s = socket.socket(socket.AF_INET, socket.SOCK_STREAM); s.connect((\"localhost\", 8080))') || exit 1"
          --health-interval 15s
          --health-timeout 10s
          --health-retries 10
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 📂 Download SDKs
        uses: actions/download-artifact@v4
        with:
          name: generated-sdks
          path: sdks

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: 🐍 Test Python Instantiation
        run: |
          cd tests/python
          pip install pydantic urllib3 python-dateutil typing-extensions
          python3 test_instantiation.py

      - name: 🐹 Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.23"
          cache: false

      - name: 🐹 Test Go Instantiation
        run: |
          cd sdks/go
          go mod tidy
          go build ./...
